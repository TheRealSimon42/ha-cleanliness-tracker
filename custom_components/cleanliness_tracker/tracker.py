"""Per-room tracker that owns presence state and computes score updates."""

from __future__ import annotations

# TODO(phase-1.3): implement RoomTracker (on_presence_start/end, periodic_update,
# mark_cleaned, set_score).
