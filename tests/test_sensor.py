"""Tests for the score + last-cleaned sensors."""

from __future__ import annotations

# TODO(phase-1.5): integration test (setup -> presence change -> score grows ->
# mark_cleaned -> score back to 0).
